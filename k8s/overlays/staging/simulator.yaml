apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simulator-ingress
spec:
  tls:
    - hosts:
        - rorla-engine-stage.predictivemovement.se
      secretName: simulator-stage-tls
  rules:
    - host: rorla-engine-stage.predictivemovement.se